namespace ExcelArchive.Api.Common.Http;

/// <summary>Shared session-cookie identity (name + policy mirror the login/logout paths).</summary>
public static class SessionCookie
{
    public const string Name = "excel_archive_session";
}
